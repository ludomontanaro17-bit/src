# version: '3.8'

services:
  app:
    image: python:latest
    container_name: my_python_app
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    working_dir: /app
    command: ["python", "main.py"]
    ports:
      - "5555:5555"
    #depends_on:
    #  - app